const nutzerNamen = ["john smith", "bob tylor"]; /* Die const -Deklaration erstellt eine Konstante */
let  isFull  =  false;                           /* let ist eine Variabel */
console.log(typeof true);                        /* console.log gibt eine Nachricht als String mit Variablen in die Consolen */
if(isFull){console.log("ist voll")}else{nutzerNamen.push("chuck peterson");}    /* Die if-Anweisung führt Anweisungen aus, wenn eine bestimmte Bedingung zu true ausgewertet wird. */
console.log();
console.log(nutzerNamen);
